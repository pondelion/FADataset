from distutils.core import setup
from setuptools import setup, find_packages


setup(
  name         = 'fin_app_dataset',
  description  = 'fin_app_dataset',
  url          = 'https://github.com/pondelion/FADataset',
  packages     = ['fin_app_dataset'],
)
